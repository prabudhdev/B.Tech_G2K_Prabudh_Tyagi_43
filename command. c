#include <stdio.h>

int main(int argc, char *argv[]) {
    printf("Program name: %s\n", argv[0]);

    if (argc < 2) {
        printf("No arguments passed.\n");
    } else {
        printf("First argument: %s\n", argv[1]);
    }

    return 0;
}
